# 0.1.0 发布验收总结

[English](../../release-acceptance/0.1.0.md)

记录日期：2026-09-11。结论：按本组已约定的非 Developer ID 验收范围，前 9 项工作完成，本组关闭。正式公开发布仍待下述发布环节完成。本记录采用本次十项计划的编号；早期报告中的其他编号不改变其实际测试内容。

## 最终候选与证据归属

| 身份 | 值 |
|---|---|
| 源码提交 | `8b374ff0e993e66cf426b1aee6a633b086f9958d` |
| 源码树 | `415485847240e67957a6c4f410161752b84e7312` |
| 冻结源码状态 | clean dev |
| 版本 / 原始构建号 | 0.1.0 / 1 |
| MLX 提交 | `73ad5df20cb30be4192e5c4d0ae8130674773427` |
| 平台 / 最低系统 | arm64 / macOS 26.4 |
| ZIP SHA-256 | `bb602e4053441941ebca9b1b20b6c62850a74cc73ed50ca86806abb8b15de447` |
| 签名 | local ad-hoc，未作 Developer ID 签名或公证 |
| CI | [34572778470](https://github.com/apepkuss/ironmlx/actions/runs/34572778470)，本次查询确认 completed / success，headSha 与最终候选一致 |

冻结包及逐文件清单位于 `dist/candidates/8b374ff0e/`。本次重新核验其 `SHA256SUMS` 全部通过，并核验 M5 完整升级证据的 `SHA256SUMS` 全部通过。原始冻结记录中的 CI in_progress、M1 pending 是当时快照，由本关闭记录补充，不改写原始证据。

验收随缺陷修复分阶段进行，并非在最终提交全量重跑一次。完整模型矩阵属于 `ebd1a338b`；恢复与压力复验属于 `046509dae`；调度器、下载界面、LAN 和固定状态修复各有后续定向回归。M5 完整升级目标 `b1a022ff2` 与最终 `8b374ff0e` 的 Git 源码树一致，本次已重新核对；这不意味着两个 Bundle 的元数据或二进制逐字一致。M1 完整升级直接验收最终 `8b374ff0e`。

## 十项完成矩阵

| 项 | 工作 | 关闭结果与主要证据 |
|---|---|---|
| 1 | 更新方式与范围 | 完成：首版采用 Sparkle 自动更新；Apple Silicon、macOS 26.4 起；协议和模型范围以已验收矩阵为准。双机独立推理与 LAN 分列 |
| 2 | 产物身份 | 完成：clean 源码、版本、tag 和 Bundle 身份门禁已实现；本地候选不冒充正式 stable tag。见 `scripts/verify-release-identity.py` 及最终候选 identity/manifest |
| 3 | 材料路径与归档 | 完成：ZIP/DMG 材料与归档检查已实现，最终 ZIP 冻结校验通过。见 `scripts/release-archives.py`、候选 archive-verification.log |
| 4 | 自动更新方案 | 完成非 Apple 凭据范围：通道、feed、Ed25519 签名及拒绝路径；双机完整 App 下载、安装、退出重启和数据保留通过。正式公开 feed 验收另列 |
| 5 | 稳定版流水线 | 完成实现与无凭据检查；精确提交构建、归档传递和发布门禁已集成。阶段 CI 34192812875 的通过记录及当前 CI 为依据；模拟 Apple/GitHub 测试不等于生产签名发布。见 [流水线说明](../stable-release-pipeline.md) |
| 6 | 分发材料审查 | 完成约定审查，随包材料与候选绑定；模型由用户另行下载。公开分发开关按约定保持 false。见 [材料审查](../distribution-review.md) 及候选 Legal 目录；历史审查哈希只对应其注明的快照 |
| 7 | 冻结候选与 CI | 完成：最终候选身份见上表，CI success；模型快照证据随各轮测试保存 |
| 8 | 真模型与协议 | 完成阶段验收：13 个基座、三协议、HTTP/SSE、结构化输出、工具循环、推测路径共 724/724；另有后续问题修复的定向回归。见 `dist/acceptance/ebd1a338b/step8/README.md` |
| 9 | 恢复、安全与硬件 | 完成约定范围：双机本地推理、最低系统、生产 helper 恢复、下载续传与完整性、Keychain、双向 LAN、受控并发/内存压力、双机完整升级。证据层级及限制见下文 |
| 10 | 汇总与关闭 | 本记录与本地证据索引完成；正式发布待办和未定位事件保留，不将其写成已通过 |

## 硬件、系统与独立验收

| 设备 | 系统 | 本地验收依据 |
|---|---|---|
| M5 Max / 128 GiB | macOS 26.4 (25E246) | 本机直接执行模型/协议、恢复、安全、受控压力和完整自动升级；各轮候选身份分别保留 |
| M1 Pro / 32 GB | macOS 26.4 | 用户实机验收与回传报告：独立推理、恢复、32K 请求、最终候选升级及数据保留通过 |

M1 升级回传报告原先写作 macOS 26.6.2；用户在关闭前明确更正实际系统为 macOS 26.4，本汇总采用该更正。因此最低系统实机验证已覆盖。远端原始报告的版本字段差异在此保留说明，不将用户回传结果表述为本机直接重跑。

M5 受控压力记录为两分钟双并发 548/548 成功、70,144 输出 tokens；过载请求按预期拒绝。内存准入修复后的两种上限测试通过。没有制造整机 OOM，没有覆盖 emergency 水位或长期稳定性，也不把这组数据推广为所有模型的性能保证。

下载证据包括实际中断后复用 37,209,920 bytes、最终 712,575,975 bytes / 6 文件校验；缺失与同大小损坏拒绝。Keychain 包括跨进程持久化、轮换回退及升级前后凭证哈希验证。详细范围见 `dist/acceptance/046509dae/step9-m5/README.md`，其中尚未执行 M1/LAN/升级的描述属于历史阶段状态，由本汇总补充。

## 双向 LAN（独立于本地推理）

M5 客户端 → M1 服务端：本机保存的响应与连接证据覆盖认证/证书、真实推理与 SSE、取消、并发、轮换、关闭 LAN、IP 变化后手动恢复，以及同 IP 断网后的新请求恢复。原始记录位于 `/Users/xin/workspace/test-ironmlx-lan/results-a/` 与 `results-same-ip-9825c8256/`；服务端本机可用性另由用户确认。

M1 客户端 → M5 服务端：用户在本次会话逐项确认认证/证书、推理/SSE、取消、并发、轮换、关闭 LAN 后本机仍可用，以及同 IP 断网恢复通过。该方向为用户实机确认，本地未收齐全部远端原始日志。双向 LAN 已按会话确认关闭，不用其结果替代另一台机器的独立本地推理，也不声称断网中的流可续传。

## 完整自动升级与修复闭环

M5：`d2055760a` → `b1a022ff2`，测试构建号 201 → 202。无效签名拒绝、下载中断保留原安装、成功替换与新进程启动均通过；586 次配置采样一致，13 个模型文件、配置/参数和凭证哈希不变，原 CA/Key 推理通过。见 `dist/acceptance/b1a022ff2/full-update/README.md`。

M1：`d2055760a` → `8b374ff0e`，测试构建号 201 → 202。用户回传报告确认三用例通过；成功升级 717 次配置采样一致，12 个模型文件、配置/参数和凭证哈希不变，目标固定模型恢复，原 CA/Key 推理通过，About 显示 build 202。临时测试源、证书信任、私钥和备份已清理，原冻结 App 恢复并再次验收通过。远端证据目录为 `/Users/sam/Applications/IronMLX-Update-Validation-20260911`。

两机均使用本地 HTTPS feed、测试签名和完整 App 派生副本；这些证据证明生产更新管理器的本地更新流程，不替代公开渠道验证。旧版启动可能先丢失固定状态的问题已记录；脚本仅在释放更新前恢复基线，更新期间不人工干预。原失败证据 `dist/acceptance/d2055760a/full-update/` 保留，修复后通过记录独立保存。

## 未定位事件与正式发布待办

1. **单次安装失败，原因未确认；Applications 路径复验通过。** M1 原 Desktop 测试在 EdDSA 验证成功、App 退出后，`renamex_np` 返回 `EDEADLK` / Code 11。移动测试目录并复用签名产物后通过。不能据此认定 TCC 拒绝、Desktop 必然失败或产品源码缺陷；原失败日志保留。建议后续同包受控复现，本组关闭不以此为阻塞项。
2. **Developer ID 正式签名、公证、stapling 与 Gatekeeper 实测待完成。** 基础脚本已具备，需实际凭据和正式产物执行。
3. **正式发布与公开 feed 端到端验收待完成。** 包括正式 tag/版本、最终材料复核、按既定安排开启分发门禁、真实资产上传下载哈希验证，以及从公开源安装升级。此次不改变公开分发开关。

本组关闭表示约定开发及本地/双机验收已完成，保留上述证据边界；不表示正式公开分发已经获验收。本次只新增汇总文档与证据索引，没有修改冻结包、产品代码或原始测试结果。
