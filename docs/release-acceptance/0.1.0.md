# 0.1.0 Release Acceptance Summary

[简体中文](../zh-CN/release-acceptance/0.1.0.md)

Recorded on 2026-09-11. Conclusion: steps 1–9 are complete within the agreed acceptance scope that does not require Developer ID credentials, and this work group is closed. Public release still requires the release steps listed below. This record uses the numbering of the current ten-step plan; different numbering in earlier reports does not change what those reports actually tested.

## Final candidate and evidence attribution

| Identity | Value |
|---|---|
| Source commit | `8b374ff0e993e66cf426b1aee6a633b086f9958d` |
| Source tree | `415485847240e67957a6c4f410161752b84e7312` |
| Frozen source state | clean dev |
| Version / original build number | 0.1.0 / 1 |
| MLX commit | `73ad5df20cb30be4192e5c4d0ae8130674773427` |
| Platform / minimum OS | arm64 / macOS 26.4 |
| ZIP SHA-256 | `bb602e4053441941ebca9b1b20b6c62850a74cc73ed50ca86806abb8b15de447` |
| Signing | local ad-hoc; no Developer ID signing or notarization |
| CI | [34572778470](https://github.com/apepkuss/ironmlx/actions/runs/34572778470), queried during closure and confirmed completed / success, with headSha matching the final candidate |

The frozen package and per-file manifest are in `dist/candidates/8b374ff0e/`. All entries in its `SHA256SUMS` were verified again during closure, as were all entries in the M5 full-update evidence `SHA256SUMS`. The original freeze record's CI in_progress and M1 pending fields are historical snapshots. This closure record supplements them without rewriting the original evidence.

Acceptance proceeded in stages as defects were fixed; the entire matrix was not rerun on the final commit. The full model matrix belongs to `ebd1a338b`, and recovery and pressure retesting to `046509dae`. Scheduler, download UI, LAN, and model-pin fixes have subsequent targeted regressions. The M5 full-update target `b1a022ff2` and final `8b374ff0e` have the same Git source tree, rechecked during closure; this does not imply byte-identical Bundle metadata or binaries. M1 full-update acceptance directly used final `8b374ff0e`.

## Ten-step completion matrix

| Step | Work | Closure result and primary evidence |
|---|---|---|
| 1 | Update method and scope | Complete: Sparkle automatic updates for the first release; Apple Silicon, macOS 26.4 or later; protocols and models limited to the accepted matrix. Independent inference on each machine and LAN acceptance are recorded separately |
| 2 | Artifact identity | Complete: clean-source, version, tag, and Bundle identity gates implemented. Local candidates are not presented as official stable tags. See `scripts/verify-release-identity.py` and the final candidate identity/manifest |
| 3 | Materials paths and archives | Complete: ZIP/DMG materials and archive checks implemented; final frozen ZIP verification passed. See `scripts/release-archives.py` and candidate archive-verification.log |
| 4 | Automatic updates | Complete without Apple credentials: channels, feed, Ed25519 signing, and rejection paths; full App download, installation, exit/restart, and data preservation passed on both machines. Public-feed acceptance is listed separately |
| 5 | Stable release pipeline | Implementation and checks without credentials complete; exact-commit builds, archive transfer, and release gates integrated. Evidence includes the recorded passing stage CI 34192812875 and current CI; simulated Apple/GitHub tests do not constitute production signed-release acceptance. See [pipeline documentation](../stable-release-pipeline.md) |
| 6 | Distribution materials review | Agreed review complete, with bundled materials bound to the candidate; users download models separately. Public distribution remains false as agreed. See [materials review](../distribution-review.md) and the candidate Legal directory; historical review hashes apply only to their stated snapshots |
| 7 | Candidate freeze and CI | Complete: final identity above, CI success; model snapshot evidence retained with each test round |
| 8 | Real models and protocols | Staged acceptance complete: 13 base models, three protocols, HTTP/SSE, structured output, tool loops, and speculative paths, 724/724 passed; later fixes have targeted regressions. See `dist/acceptance/ebd1a338b/step8/README.md` |
| 9 | Recovery, security, and hardware | Agreed scope complete: independent inference on both machines, minimum OS, production helper recovery, download resume and integrity, Keychain, bidirectional LAN, controlled concurrency/memory pressure, and full updates on both machines. Evidence levels and limits follow below |
| 10 | Summary and closure | This record and the local evidence index are complete; remaining release work and the unresolved incident are retained rather than marked passed |

## Hardware, OS, and independent acceptance

| Device | OS | Local inference acceptance basis |
|---|---|---|
| M5 Max / 128 GiB | macOS 26.4 (25E246) | Model/protocol, recovery, security, controlled pressure, and full automatic-update tests executed directly on this machine; each round retains its candidate identity |
| M1 Pro / 32 GB | macOS 26.4 | User-performed device acceptance and returned report: independent inference, recovery, 32K requests, final-candidate update, and data preservation passed |

The returned M1 update report originally stated macOS 26.6.2. Before closure, the user explicitly corrected the actual OS to macOS 26.4; this summary adopts that correction. Minimum-OS device validation is therefore covered. The discrepancy in the remote report's OS field is documented here, and user-reported results are not presented as tests rerun directly on this machine.

M5 controlled pressure recorded 548/548 successful requests and 70,144 output tokens over two minutes with concurrency two; overload requests were rejected as expected. Both limit tests passed after the memory-admission fix. No system-wide OOM was induced, emergency levels and long-term stability were not covered, and these results are not a performance guarantee for every model.

Download evidence includes reuse of 37,209,920 bytes after a real interruption and final verification of 712,575,975 bytes across six files; missing files and same-size corruption were rejected. Keychain coverage includes cross-process persistence, rotation rollback, and credential-hash verification before and after updates. See `dist/acceptance/046509dae/step9-m5/README.md` for detailed scope. Its statements that M1/LAN/updates had not yet run describe an earlier stage and are supplemented by this summary.

## Bidirectional LAN, separate from local inference

M5 client → M1 server: locally retained responses and connection evidence cover authentication/certificates, real inference and SSE, cancellation, concurrency, rotation, disabling LAN, manual recovery after an IP change, and new-request recovery after a network interruption with the same IP. Original records are in `/Users/xin/workspace/test-ironmlx-lan/results-a/` and `results-same-ip-9825c8256/`; the user separately confirmed server-local availability.

M1 client → M5 server: during this session, the user confirmed authentication/certificates, inference/SSE, cancellation, concurrency, rotation, local availability after disabling LAN, and recovery after a network interruption with the same IP. This direction is based on user device confirmation; not all remote raw logs have been collected locally. Bidirectional LAN acceptance is closed based on these confirmations. It does not replace independent inference acceptance on either machine or claim that interrupted streams can resume.

## Full automatic updates and fix verification

M5: `d2055760a` → `b1a022ff2`, test build 201 → 202. Invalid signatures were rejected, interrupted downloads preserved the installed App, and successful replacement and new-process startup passed. All 586 configuration samples matched; hashes of 13 model files, configuration/parameters, and credentials were unchanged; inference with the original CA/Key passed. See `dist/acceptance/b1a022ff2/full-update/README.md`.

M1: `d2055760a` → `8b374ff0e`, test build 201 → 202. The returned user report confirms all three cases passed. All 717 configuration samples during the successful update matched; hashes of 12 model files, configuration/parameters, and credentials were unchanged. The pinned target model was restored, inference with the original CA/Key passed, and About displayed build 202. Temporary test servers, certificate trust, private keys, and backups were cleaned up; the original frozen App was restored and passed acceptance again. The remote evidence directory is `/Users/sam/Applications/IronMLX-Update-Validation-20260911`.

Both machines used local HTTPS feeds, test signatures, and derived copies of complete Apps. This evidence validates the production update manager's local update flow, not public-channel acceptance. The old version's possible loss of pins at startup was recorded; scripts restored the baseline only before releasing the update, with no manual intervention during the update. Original failure evidence in `dist/acceptance/d2055760a/full-update/` is retained separately from passing post-fix records.

## Unresolved incident and remaining release work

1. **One installation failure, cause unconfirmed; retest under Applications passed.** In the original M1 Desktop test, after successful EdDSA verification and App exit, `renamex_np` returned `EDEADLK` / Code 11. Moving the test directory and reusing the signed artifacts succeeded. This does not establish TCC denial, inevitable Desktop failure, or a product source defect. Original failure logs are retained. A controlled reproduction with the same package is recommended later and does not block this work group's closure.
2. **Production Developer ID signing, notarization, stapling, and Gatekeeper validation remain pending.** Supporting scripts exist; execution requires actual credentials and production artifacts.
3. **Production release and public-feed end-to-end acceptance remain pending.** This includes the official tag/version, final materials review, enabling the distribution gate according to the agreed release arrangement, actual asset upload/download hash verification, and installation/update from the public source. The public distribution switch is unchanged.

Closure means the agreed development and local/two-machine acceptance work is complete within the evidence boundaries above; it does not mean public distribution has passed acceptance. This closure work adds only summary documentation and an evidence index, without changing frozen packages, product code, or original test results.
